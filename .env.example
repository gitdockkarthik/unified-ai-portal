# Anthropic API Key — required for AI chatbot features
ANTHROPIC_API_KEY=sk-ant-...

# Database (auto-configured in Docker Compose)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/unified_ai_portal

# Railway deployment — set DATABASE_URL to Railway's PostgreSQL URL
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname
